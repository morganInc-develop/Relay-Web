{
  "name": "relay-web",
  "private": true,
  "scripts": {
    "dev": "npm --prefix apps/platform run dev",
    "lint": "npm --prefix apps/platform run lint",
    "build": "npm --prefix apps/platform run build",
    "format": "npm --prefix apps/platform run format",
    "vercel-build": "npm --prefix apps/platform install --include=dev && npm --prefix apps/platform run prisma:generate && rm -rf node_modules && ln -s apps/platform/node_modules node_modules && npm --prefix apps/platform run build"
  }
}

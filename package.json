{
  "name": "relay-web",
  "private": true,
  "scripts": {
    "vercel-build": "npm --prefix apps/platform install && npm --prefix apps/platform run prisma:generate && rm -rf node_modules && ln -s apps/platform/node_modules node_modules && npm --prefix apps/platform run build"
  }
}
